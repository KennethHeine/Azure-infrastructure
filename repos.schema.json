{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Repository onboarding configuration",
  "type": "object",
  "required": ["repos"],
  "properties": {
    "gitHubOrg": {
      "type": "string",
      "description": "GitHub organization or user that owns the repos"
    },
    "location": {
      "type": "string",
      "description": "Azure region for resource groups"
    },
    "repos": {
      "type": "array",
      "description": "List of repository names to onboard",
      "items": {
        "type": "string",
        "description": "Repository name (used to derive rg-<name> and sp-<name>-github)"
      }
    }
  }
}
